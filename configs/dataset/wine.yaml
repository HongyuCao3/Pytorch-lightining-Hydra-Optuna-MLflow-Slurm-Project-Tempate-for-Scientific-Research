_target_: src.data.wine_datamodule.WineDataModule

data_dir: data/
batch_size: 32
num_workers: 0
val_split: 0.2
test_split: 0.1
# Seed used for random_split of train/val/test. Must be identical across
# every eval run and every baseline for comparisons to be meaningful.
# Logged as an MLflow tag by run_train / run_eval.
split_seed: 42
