# Experiment ops

- Estimate runtime before proposing long runs.
- Use config values to estimate total steps and wall-clock time.
- For notifications, generate a wrapper script that:
  - records start time
  - traps EXIT and ERR
  - sends an email with status, duration, and log path
- Never hardcode secrets. Use environment variables.